# proto/github.com/prometheus/client_model/go/metrics.pb.go  
**Package name**    
`io_prometheus_client`  
  
### Imports  
```go  
import proto "github.com/golang/protobuf/proto"  
import fmt  "fmt"  
import math "math"  
```  
The file is generated by `protoc-gen-go` from the source `metrics.proto`.  
  
---  
  
## External data sources  
* The protobuf descriptor for this file is stored in the byte slice `fileDescriptor8`.    
  It contains a gzipped `FileDescriptorProto` that describes all types defined below.  
  
---  
  
## TODO comments  
No explicit `TODO:` markers are present in the generated code.  
  
---  
  
# Major code parts  
  
### MetricType enum  
Defines an enumeration for metric kinds (`COUNTER`, `GAUGE`, `SUMMARY`, `UNTYPED`, `HISTOGRAM`) with helper methods:  
* `Enum()` – returns a pointer to a new value.  
* `String()` – serialises the enum name.  
* `UnmarshalJSON()` – unmarshals JSON into an enum value.  
* `EnumDescriptor()` – provides descriptor information.  
  
### LabelPair struct  
Represents a single label key/value pair for a metric.    
Fields: `Name`, `Value`.    
Methods provide reset, string conversion, protobuf registration and getters for each field.  
  
### Gauge struct  
Holds the gauge value of a metric (`float64`).    
Includes standard protobuf helper methods and a getter for the value.  
  
### Counter struct  
Similar to `Gauge` but represents a counter.  Contains a single `Value` field with helpers.  
  
### Quantile struct  
Used inside a summary; holds a quantile point and its value.    
Fields: `Quantile`, `Value`.  Getters return each field.  
  
### Summary struct  
Represents a Prometheus summary metric:  
* `SampleCount` – number of samples.  
* `SampleSum` – sum of all samples.  
* `Quantile` – repeated list of `Quantile` entries.    
Standard helpers and getters are provided.  
  
### Untyped struct  
A generic metric value holder with one field `Value`.  Includes reset, string, protobuf helpers and a getter.  
  
### Histogram struct  
Represents a histogram metric:  
* `SampleCount`  
* `SampleSum`  
* `Bucket` – repeated list of `Bucket` entries (note the typo “Bucket” in the descriptor).    
Reset, string, protobuf helpers and getters are defined.  
  
### Bucket struct  
Used inside a histogram; holds cumulative count and upper bound.    
Fields: `CumulativeCount`, `UpperBound`.  Getters return each field.  
  
### Metric struct  
The core metric type that can contain any of the above sub‑metrics:  
* repeated labels (`LabelPair` slice)  
* optional gauge, counter, summary, untyped, histogram pointers  
* timestamp in milliseconds (`TimestampMs`).    
Getters expose all fields; reset/string/registration helpers are present.  
  
### MetricFamily struct  
Describes a family of metrics (a Prometheus concept):  
* `Name`, `Help`, `Type` (enum), repeated list of `Metric`.    
Standard protobuf helpers and getters for each field.  
  
### init functions  
Two `init()` blocks register all types with the protobuf runtime and register the file descriptor.  
  
---  
  
All generated code is ready to be used by other components that consume or produce Prometheus metrics.  
  
